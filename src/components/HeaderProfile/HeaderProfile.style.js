import styled from 'styled-components';

export const Wrapper = styled.div`
  .title {
    letter-spacing: .3rem;
    font-size: 3rem;
    line-height: .7;
    color: var(--red)
    }

  .jobTitle {
    font-size: 1.5rem;
    font-weight: bold;
  }
`