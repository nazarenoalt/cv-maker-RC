import styled from 'styled-components';

export const Wrapper = styled.div`
  .title {
    letter-spacing: .1em;
    font-size: 3em;
    color: var(--red)
    }

    .firstName {
      margin-bottom: .2em;
    }

    .lastName {
      margin-top: 0;
    }
  .jobTitle {
    font-size: 1.5em;
    font-weight: bold;
  }
`