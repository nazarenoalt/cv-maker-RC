import styled from 'styled-components';

export const Wrapper = styled.div`
  .title {
    letter-spacing: .1em;
    font-size: 3em;
    line-height: .7;
    color: var(--red)
    }

  .jobTitle {
    font-size: 1.5em;
    font-weight: bold;
  }
`